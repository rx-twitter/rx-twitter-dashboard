name: deploy

on:
  workflow_run:
    workflows: [push-image]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Debug webhook URL shape
        env:
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
        run: |
          python3 - <<'PY'
          import os
          url = os.environ["WEBHOOK_URL"]
          print("starts with https://deploy.t1nyb0x.cloud:", url.startswith("https://deploy.t1nyb0x.cloud"))
          print("ends with /deploy/dashboard:", url.endswith("/deploy/dashboard"))
          print("ends with slash:", url.endswith("/"))
          print("path tail:", url.split("deploy.t1nyb0x.cloud")[-1])
          PY
      - name: Trigger Deploy Webhook
        env:
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
        run: |
          set -euo pipefail

          PAYLOAD='{"event":"deploy"}'
          SIGNATURE="sha256=$(printf '%s' "$PAYLOAD" | openssl dgst -sha256 -hmac "$WEBHOOK_SECRET" -binary | xxd -p -c 256)"

          HTTP_STATUS=$(curl -sS -o /tmp/webhook_response.txt -w "%{http_code}" \
            -X POST \
            -H "Content-Type: application/json" \
            -H "X-Hub-Signature-256: $SIGNATURE" \
            --data "$PAYLOAD" \
            --max-time 15 \
            "$WEBHOOK_URL")

          echo "Response: $(cat /tmp/webhook_response.txt)"
          echo "HTTP Status: $HTTP_STATUS"

          if [ "$HTTP_STATUS" != "204" ]; then
            echo "::error::Webhook returned HTTP $HTTP_STATUS"
            exit 1
          fi

          echo "Deploy webhook triggered successfully"
